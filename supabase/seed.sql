-- Seed opcional — rode manualmente no SQL Editor do Supabase após as migrations.
-- Substitua pelo seu email antes de executar.

-- insert into public.allowed_emails (email)
-- values ('seu.email@exemplo.com')
-- on conflict (email) do nothing;
